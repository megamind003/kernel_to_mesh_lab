db.url=jdbc:postgresql://localhost:5432/chronos
db.username=postgres
db.password=postgres

node.id=node-1
node.shard.start=0
node.shard.end=9

timingwheel.tick.seconds=1
timingwheel.size=60

fetch.window.minutes=10
fetch.batch.size=1000

executor.max.concurrent.per.tenant=100

heartbeat.timeout.seconds=45
reaper.scan.interval.seconds=30
node.heartbeat.interval.seconds=10

grpc.port=50051
